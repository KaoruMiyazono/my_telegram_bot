[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -ra --ignore=tests/lifecycle
markers =
    live: requires real external APIs and is excluded from the default suite
